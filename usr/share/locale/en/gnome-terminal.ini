Terminal=Terminal
Close the terminal ?=Close the terminal ?
Close the terminal=Close the terminal
A process is still running in the terminal. Close the terminal will kill it.=A process is still running in the terminal. Close the terminal will kill it.
You are disconnected (the idle time is possibly exceeded)=You are disconnected (the idle time is possibly exceeded)
